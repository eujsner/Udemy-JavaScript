const letras = ['A', 'B', 'C', 'D', 'E', 'F', 'G']

console.log(letras.slice(1, 4))
console.log(letras.slice(1))
console.log(letras.slice(-2)) // Pega de tras para frente
console.log(letras.slice(0, -2)) // Tirando os dois ultimos
